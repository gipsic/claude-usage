## What & why

<!-- One paragraph. Link the issue if there is one: Fixes #123 -->

## How I verified it

<!-- `npm test` output, and — for anything touching limits/reset times — a comparison
     against Claude app → Settings → Usage. Real numbers from real accounts are the
     strongest evidence this project has. -->

- [ ] `npm test` passes
- [ ] New behaviour has a test, or I explained why it can't
- [ ] No new npm dependencies; nothing leaves the machine except the two Anthropic hosts
- [ ] Any estimate shown to users is labelled as such (`estimated` / `inferred`)
- [ ] CHANGELOG.md updated under *Unreleased*
